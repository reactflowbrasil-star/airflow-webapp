{
 "name": "WF-06 Gerar cobranca",
 "nodes": [
  {
   "parameters": {
    "httpMethod": "POST",
    "path": "wf-06-gerar-cobranca",
    "responseMode": "onReceived",
    "options": {}
   },
   "id": "wh-wf-06-gerar-cobranca",
   "name": "Receber",
   "type": "n8n-nodes-base.webhook",
   "typeVersion": 2,
   "position": [
    0,
    0
   ]
  },
  {
   "parameters": {
    "jsCode": "// Valida evento vindo do BACKEND (HMAC + janela de tempo)\nconst crypto = require('crypto');\nconst h = $input.item.json.headers || {};\nconst ts = h['x-airflow-timestamp'];\nconst sig = h['x-airflow-signature'];\nconst body = JSON.stringify($input.item.json.body);\nif (!ts || !sig) throw new Error('Assinatura ausente');\nif (Math.abs(Date.now()/1000 - Number(ts)) > 300) throw new Error('Timestamp fora da janela');\nconst esperado = crypto.createHmac('sha256', $env.N8N_WEBHOOK_SECRET).update(ts + '.' + body).digest('hex');\nif (esperado !== sig) throw new Error('Assinatura invalida');\nreturn { json: $input.item.json.body };"
   },
   "id": "wf-06-gerar-cobranca-val",
   "name": "Validar HMAC",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    220,
    0
   ]
  },
  {
   "parameters": {
    "jsCode": "// payment.requested → cria a cobranca NO BACKEND (registro antes do gateway)\nconst crypto = require('crypto');\nconst d = $json.data;\nreturn { json: { comando: {\n  command: 'pagamento.criar',\n  order_id: d.order_id,\n  method: 'PIX',\n  idempotency_key: 'pagamento.criar:' + d.order_id,\n  correlation_id: $json.correlation_id,\n} } };"
   },
   "id": "wf-06-gerar-cobranca-cmd",
   "name": "Montar comando",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    440,
    0
   ]
  },
  {
   "parameters": {
    "jsCode": "// Assina comando para o BACKEND (HMAC + timestamp + nonce anti-replay)\nconst crypto = require('crypto');\nconst payload = $json.comando;\nconst body = JSON.stringify(payload);\nconst ts = String(Math.floor(Date.now()/1000));\nconst nonce = crypto.randomUUID();\nconst sig = crypto.createHmac('sha256', $env.BACKEND_WEBHOOK_SECRET).update(ts + '.' + nonce + '.' + body).digest('hex');\nreturn { json: { body, ts, nonce, sig } };"
   },
   "id": "wf-06-gerar-cobranca-sig",
   "name": "Assinar",
   "type": "n8n-nodes-base.code",
   "typeVersion": 2,
   "position": [
    660,
    0
   ]
  },
  {
   "parameters": {
    "method": "POST",
    "url": "={{$env.BACKEND_BASE_URL}}/api/n8n/comandos",
    "sendHeaders": true,
    "headerParameters": {
     "parameters": [
      {
       "name": "content-type",
       "value": "application/json"
      },
      {
       "name": "x-n8n-timestamp",
       "value": "={{$json.ts}}"
      },
      {
       "name": "x-n8n-nonce",
       "value": "={{$json.nonce}}"
      },
      {
       "name": "x-n8n-signature",
       "value": "={{$json.sig}}"
      }
     ]
    },
    "sendBody": true,
    "specifyBody": "raw",
    "rawContents": "={{$json.body}}",
    "options": {
     "timeout": 15000
    }
   },
   "id": "wf-06-gerar-cobranca-http",
   "name": "Chamar backend",
   "type": "n8n-nodes-base.httpRequest",
   "typeVersion": 4.2,
   "position": [
    880,
    0
   ]
  }
 ],
 "connections": {
  "Receber": {
   "main": [
    [
     {
      "node": "Validar HMAC",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Validar HMAC": {
   "main": [
    [
     {
      "node": "Montar comando",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Montar comando": {
   "main": [
    [
     {
      "node": "Assinar",
      "type": "main",
      "index": 0
     }
    ]
   ]
  },
  "Assinar": {
   "main": [
    [
     {
      "node": "Chamar backend",
      "type": "main",
      "index": 0
     }
    ]
   ]
  }
 },
 "settings": {
  "executionOrder": "v1",
  "timezone": "America/Sao_Paulo"
 },
 "meta": {
  "description": "Ao receber payment.requested, dispara a criacao da cobranca via backend, que registra o Payment ANTES de chamar o gateway pela abstracao PaymentProvider. Idempotente por ordem."
 }
}
